{
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "Tên bài hát / bản ghi nhận dạng được"
    },
    "tempo": {
      "type": "number",
      "description": "Nhịp độ BPM"
    },
    "keySignature": {
      "type": "string",
      "enum": ["ngũ cung Bắc Bộ", "ngũ cung Nam Bộ", "chromatic", "unknown"]
    },
    "ethnicGroup": {
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "name"],
      "description": "Dân tộc — lấy từ DB Reference"
    },
    "language": {
      "type": "string",
      "enum": [
        "tiếng Tày", "tiếng Nùng", "tiếng Việt", "tiếng Chăm",
        "tiếng Thái", "tiếng H'Mông", "instrumental", "unknown"
      ]
    },
    "instruments": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "type": "string" },
          "name": { "type": "string" },
          "confidence": { "type": "number", "description": "Độ tin cậy của phân tích nhạc cụ (0.0 - 1.0)" },
          "max_confidence": { "type": "number" },
          "overall_average": { "type": "number" },
          "frame_ratio": { "type": "number" },
          "dominant_frames": { "type": "integer" },
          "total_frames": { "type": "integer" }
        },
        "required": ["id", "name", "confidence"]
      },
      "description": "Nhạc cụ — lấy từ DB Reference"
    },
    "performanceContext": {
      "type": "string",
      "enum": ["hát ru", "lễ hội", "nghi lễ", "giải trí", "lao động", "unknown"]
    },
    "ceremony": {
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "name"],
      "description": "Nghi lễ / lễ hội — lấy từ DB Reference (null nếu không xác định)"
    },
    "vocalStyle": {
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "name"],
      "description": "Phong cách giọng hát — lấy từ DB Reference (null nếu instrumental)"
    },
    "musicalScale": {
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "name"],
      "description": "Thang âm — lấy từ DB Reference (null nếu không xác định)"
    },
    "composer": {
      "type": "string",
      "description": "Tác giả (null nếu dân gian)"
    },
    "recordingLocation": {
      "type": "string",
      "description": "Vùng miền / địa phương ước đoán"
    },
    "regionSuggestion": {
      "type": "object",
      "properties": {
        "region": {
          "type": "string",
          "description": "Vùng miền chính — lấy từ Regions trong DB Reference"
        },
        "detail": {
          "type": "string",
          "description": "Mô tả chi tiết đặc trưng vùng miền nhận diện được từ audio"
        }
      },
      "required": ["region"],
      "description": "Gợi ý vùng miền dựa trên đặc trưng âm nhạc (thang âm, phong cách, nhạc cụ)"
    },
    "classification": {
      "type": "object",
      "properties": {
        "performanceType": {
          "type": "string",
          "enum": ["vocal", "instrumental", "mixed"],
          "description": "Loại trình diễn: vocal (giọng hát), instrumental (nhạc cụ), mixed (kết hợp)"
        },
        "culturalContext": {
          "type": "string",
          "description": "Bối cảnh văn hóa (lễ hội, nghi lễ, sinh hoạt, giải trí...)"
        },
        "tags": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Các nhãn phân loại — lấy name từ Tags trong DB Reference"
        }
      },
      "required": ["performanceType"],
      "description": "Phân loại tổng hợp bài hát / bản ghi"
    },
    "overallConfidence": {
      "type": "number",
      "description": "Độ tin cậy tổng thể của toàn bộ phân tích (0.0 - 1.0). Tính dựa trên chất lượng audio, độ rõ ràng của đặc trưng âm nhạc, và mức độ chắc chắn của từng phán đoán."
    }
  },
  "required": [
    "tempo",
    "keySignature",
    "ethnicGroup",
    "language",
    "instruments",
    "performanceContext",
    "regionSuggestion",
    "classification",
    "overallConfidence"
  ]
}